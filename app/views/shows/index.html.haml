-# frozen_string_literal: true

.container
    .display-4.pt-3 Smartflix
    .h4.mt-2.fw-light Find out more about your favourite movies.

    .row{:style => "--bs-gutter-x: 0;"}
        - @shows.each do |show|
            .card.mt-4.mx-4{:style => "width: 14rem;"}
                %img.card-img-top{:alt => "placeholder image", :src => "https://via.placeholder.com/150"}
                .card-body
                    .h5.card-title= "#{show.title} (#{show.release_year})"
                    %p.card-show-type= show.show_type
                    %p.card-text= show.description
                    .star-button= button_to_toggle_show_starred(show)